# Composition — running notes

Scratch space while reading the code. Free to edit freely.

## Task sampling (`data.py`)

Uses a confif file to adjust the domain sizes and nuumber of sequences etc
Uses offset so the domains are disjoint. This helps to disambiguate between x,f(x) x,g(x) and x,h(x). 
This is clever in my opinion.

g looks like: g = [
  [g(0), g(1), ..., g(49)],   # task 0, length X_SIZE=50
  [g(0), g(1), ..., g(49)],   # task 1
  ...
  [g(0), g(1), ..., g(49)],   # task num_tasks-1
]


_build_sequence outputs the following:

One sequence of "tokens" which are generated by offesetting the lookup tables outputs corresponding to a single function.

lables corresponds to the true values of that we want to optimize over. x corresponds to -100 and y corresponds to the matching output token. In hugging face, setting a value to -100 means ignore the entry.
## Sequence construction

-
-
-

## Training

-
-
-

## Probe / patching

-
-
-

## Architecture lock (for C_GG / OOD check)

Write-up (`composition_rational_v2`) does not specify architecture; use Wurgaft-matched defaults for now:

- GPT-NeoX: **8L / 1H / d=64 / ff=256**
- AdamW lr **5e-4**, **inverse_sqrt**, warmup **500**, batch **128**
- Task sizes unchanged: |X|=|Y|=50, |Z|=5 (ΔK > 0 bottleneck)
- Train: `comp` sequences only; eval logs ID/OOD × {comp, g_only, f_only}
- Seed: 1; `num_ood_tasks`: 256; `n_eval`: 512; `eval_steps`: 1000

## Current experiments (submitted 2026-07-21)

**Goal:** In the high-D / moderate-N regime where notes predict `C_GG` can emerge, check whether we actually get **low `ood_comp` error** (precondition for composition). Probe only after OOD looks good. Inspect **error vs step**, not only the final checkpoint.

**Why:** Prior runs (D=4 / D=128 with 2L-4H-128d, lr=0.001, constant LR) had low ID error but ~90%+ OOD error → memorization (`M`), not `C_GG`/`G`.

**SCC jobs** (`qsub` via `train_scc.sh` + `EXTRA_ARGS` locking the architecture above):

| Job ID   | Name         | D    | max_steps | Status (at submit follow-up) |
|----------|--------------|------|-----------|------------------------------|
| 6815633  | `comp_D512`  | 512  | 100000    | running                      |
| 6815634  | `comp_D1024` | 1024 | 100000    | running                      |
| 6815636  | `comp_D2048` | 2048 | 100000    | running                      |

**Expected run dirs** (under `$CACHE_DIR/composition/runs/`, `CACHE_DIR=/projectnb/buinlp/rathin/cache`):

- `D512-8L-1H-64d-256ff-lr0.0005-bs128-100000steps-seed1`
- `D1024-8L-1H-64d-256ff-lr0.0005-bs128-100000steps-seed1`
- `D2048-8L-1H-64d-256ff-lr0.0005-bs128-100000steps-seed1`

**Logs:** `composition/logs/<JOB_ID>_<JOB_NAME>.log`

**After finish:**

```bash
python report_errors.py $CACHE_DIR/composition/runs/D1024-8L-1H-64d-* --all-steps
```

Success signal: `ood_comp` error clearly below ~90% (chance ~98%) for some stretch of training.

## Misc / open questions

- Ask before submitting SCC jobs.
- Architecture still not fixed in the write-up; defaults above are provisional.
-
